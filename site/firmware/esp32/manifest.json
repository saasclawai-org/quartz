{
  "name": "Quartz Miner",
  "version": "0.3.2",
  "builds": [
    {
      "chipFamily": "ESP32",
      "parts": [{ "path": "../../downloads/quartz-esp32-merged.bin", "offset": 0 }]
    },
    {
      "chipFamily": "ESP32-S3",
      "parts": [{ "path": "../../downloads/quartz-s3-merged.bin", "offset": 0 }]
    },
    {
      "chipFamily": "ESP32-C3",
      "parts": [{ "path": "../../downloads/quartz-c3-merged.bin", "offset": 0 }]
    }
  ],
  "notes": "0.3.2: full merged images (bootloader + partition table + app) — same documented release set as the Flasher page. The previous entries wrote app-only images at offset 0, which cannot boot. v075-c3 (serial console keyboard input) remains available as a manual download from /firmware/ until it ships as a merged image."
}
